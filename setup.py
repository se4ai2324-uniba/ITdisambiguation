from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Image text disambiguation for connecting text and images',
    author='itdis team',
    license='MIT',
)
